---
import BasicChart from "@/components/chart.astro";

interface Props {
    chartID: string;
    datapoints: string[];
}

let { chartID, datapoints } = Astro.props;

let charts = {};
---

<BasicChart datapoints={datapoints} chartID={chartID} datasetTypesLabel={"Algorithm Name"} datasetTypes={["standard", "optimized", "zeroes", "polynomial"]}/>