services:
  astro-app:
    # image: node:lts-alpine
    working_dir: /apps/astro-app
    build:
      context: ../
      dockerfile: apps/astro-app/Dockerfile.dev
      target: runtime
    # volumes:
    #   - astro_node_modules:/apps/astro-app/node_modules
    ports:
      - 4321:4321

  sql-receptionist:
    # image: gcc:latest
    working_dir: /apps/sql-receptionist
    build:
      context: ../
      dockerfile: apps/sql-receptionist/Dockerfile.dev
      target: dev
    ports:
      - 2523:2523


    # docker save???